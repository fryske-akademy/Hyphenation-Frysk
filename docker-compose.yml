version: "3.7"
services:
    hyphenation-frysk:
       image: "${APPNAME}:${VERSION}"
       deploy:
          replicas: 1
          resources:
             limits:
                memory: "3GB"
       ports:
          - "10003:3838"
       networks:
          - ${APPNAME}
networks:
    hyphenation-frysk:
